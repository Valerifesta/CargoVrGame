using UnityEngine;

public class Seat_Test0 : Seat
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    public override void Start()
    {
        base.Start();
        seatName = "Test Seat : Model 0";
    }

}
