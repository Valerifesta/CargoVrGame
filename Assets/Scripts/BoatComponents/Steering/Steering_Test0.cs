using UnityEngine;

public class Steering_Test0 : SteeringWheel
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public override void Start()
    {
        base.Start();
        steeringWheelName = "Test wheel : Series 0";
        /*
        maxSteerAbsolute = 10;
        steerStrength = 2.0f;
        */
    }

    
}
